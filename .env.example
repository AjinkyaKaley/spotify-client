# Spotify API Configuration
# Get your Client ID from: https://developer.spotify.com/dashboard

# Spotify Client ID (required)
VITE_CLIENT_ID=spotify_client_id_here

# Redirect URI (must match the URI registered in your Spotify app settings)
# For local development:
VITE_REDIRECT_URI=http://127.0.0.1:5173/callback

# For Vercel production deployment, set this in Vercel's Environment Variables:
# VITE_REDIRECT_URI=https://your-app.vercel.app/callback
#
# IMPORTANT FOR VERCEL DEPLOYMENT:
# 1. In Vercel Dashboard > Settings > Environment Variables, add:
#    - VITE_CLIENT_ID with your Spotify Client ID
#    - VITE_REDIRECT_URI with your production URL (e.g., https://your-app.vercel.app/callback)
# 2. Make sure these variables are enabled for "Production", "Preview", and "Development" environments
# 3. Redeploy your app after adding environment variables
# 4. In your Spotify App Settings (https://developer.spotify.com/dashboard), add your Vercel URL
#    to the "Redirect URIs" list (e.g., https://your-app.vercel.app/callback)
